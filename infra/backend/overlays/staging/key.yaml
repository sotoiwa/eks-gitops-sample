apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: backend
  namespace: backend
spec:
  data:
  - key: mydb/staging
    name: username
    property: username
  - key: mydb/staging
    name: password
    property: password